export enum Page {
  LOGIN = '/login',
  MAIN = '/main',
  REDIRECT = "/"
}

export enum ButtonType {
  Default = 'default',
  Filled = 'filled',
  Border = 'border'
}

export enum Icon {
  Add = 'add',
  Cancel = 'cancel',
  Up = 'up',
  Left = 'left',
  Down = 'down',
  Right = 'right',
  List = 'list',
  Options = 'options',
  Setting = 'setting',
  Check = 'check',
  Remove = 'remove',
  LetterSize = 'letter-size',
  LogOut = 'logout'
}